# ==========================================
# Core Runtime
# ==========================================
NODE_ENV=development
ADMIN_KEY=change-me
AUTH_SECRET=change-me
DATA_MODE=remote

# ==========================================
# Postgres (app uses only DATABASE_URL)
# ==========================================
# Required for remote mode. Neon/Vercel may also set PGHOST, POSTGRES_*, etc.;
# the app does not read those — only DATABASE_URL.
DATABASE_URL=postgresql://user:pass@host:5432/dbname?sslmode=require

# ==========================================
# Google (optional)
# ==========================================
# Admin backup preview/import + geocoding
REMOTE_CSV_URL=
GOOGLE_SHEET_ID=
GOOGLE_SERVICE_ACCOUNT_KEY=
GOOGLE_SERVICE_ACCOUNT_FILE=service-account.json
GOOGLE_MAPS_API_KEY=

# ==========================================
# Cache (optional overrides; defaults in code)
# ==========================================
# CACHE_DURATION_MS=3600000
# REMOTE_REFRESH_INTERVAL_MS=1800000
# LOCAL_CSV_LAST_UPDATED=

# ==========================================
# Public
# ==========================================
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_BASE_PATH=
