# ==========================================
# Core
# ==========================================
NODE_ENV=development
ADMIN_KEY=change-me
AUTH_SECRET=change-me
DATA_MODE=remote

# ==========================================
# Postgres (app uses only DATABASE_URL)
# ==========================================
# Connection string includes user, password, host, port, db. Neon/Vercel may set
# PGHOST, POSTGRES_*, etc.; the app does not read those.
DATABASE_URL=postgresql://user:pass@host:5432/dbname?sslmode=require

# ==========================================
# Public
# ==========================================
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_BASE_PATH=

# ==========================================
# Geocoding (map coordinates)
# ==========================================
# Enable Geocoding API in Cloud Console. If unset, arrondissement centre is used.
GOOGLE_MAPS_API_KEY=

# ==========================================
# Admin backup – Option A: Sheet API (prefer env var for remote)
# ==========================================
# For "Import / Preview Google Backup" via Sheets API. Prefer KEY (works on Vercel etc.);
# FILE is for local dev only (path to JSON key file, e.g. scripts/service-account.json).
GOOGLE_SHEET_ID=
GOOGLE_SERVICE_ACCOUNT_KEY=
# GOOGLE_SERVICE_ACCOUNT_FILE=service-account.json

# ==========================================
# Admin backup – Option B: CSV URL
# ==========================================
# Alternative to Sheet API: direct CSV URL (e.g. Publish to web from a Sheet).
REMOTE_CSV_URL=

# ==========================================
# Cache (optional; defaults in code)
# ==========================================
# CACHE_DURATION_MS=3600000
# REMOTE_REFRESH_INTERVAL_MS=1800000
# LOCAL_CSV_LAST_UPDATED=
